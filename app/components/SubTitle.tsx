import React from 'react'

const SubTitle = ({title}:{title:string}) => {
  return (
    <div className='text-yellow-400 p-2   text-xs font-semibold  text-center'>{title}</div>
  )
}

export default SubTitle